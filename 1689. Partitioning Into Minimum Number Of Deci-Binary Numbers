class Solution {
    public int minPartitions(String n) {
        int max = Integer.MIN_VALUE;
        for(int i=0;i<Math.min(n.length(),20);i++){
            max = Math.max(max,n.charAt(i)-'0');
            if(max==9)
                return max;
            
        }
        return max;
    }
}
